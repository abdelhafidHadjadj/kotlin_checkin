package com.example.first_app

object Routes {
        var loginScreen = "login"
        var homeScreen = "home"
        var settingsScreen: String = "settings"
        var permissionScreen = "permission"
        var scannerScreen = "scanner/{type}"
}

object  SettingsRoute {
        var main = "settings/"
        var profile = "profile"
        var app = "app"
}